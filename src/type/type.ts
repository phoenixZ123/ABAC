export enum UserRole {
    OPERATOR = 'operator',
    USER = 'user',
    DRIVER = 'driver',
    ADMIN = 'admin'
}

export enum OperatorPosition {
    EDITOR = 'editor',
    VIEWER = 'viewer',
    MANAGER = 'manager',
}